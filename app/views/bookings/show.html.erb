<div class="medium-banner" style="background-image: linear-gradient(-225deg, rgba(0,101,168,0.6) 0%, rgba(0,36,61,0.6) 50%), url('https://images.unsplash.com/photo-1457979406492-d1e6b97f3f55?ixlib=rb-0.3.5&s=df633aa537ab522c5e97ccae06fa3ad8&auto=format&fit=crop&w=1050&q=80');">
  <div class="banner-content">
    <h1>Booking details</h1>
  </div>
</div>

<div class="container">
  <% if current_user == @booking.flight.rocket.user %>
    <div class="message">
      <div class="message-name">
        <h2>Departure date</h2>
        <h3><%= @booking.flight.date.strftime('%a %e %b %Y') %></h3>
      </div>
      <div class="message-body">
        <h2>🛫 <%= @booking.flight.departure %></h2>
        <h2>🛬 <%= @booking.flight.destination %></h2>
        <p>Booking reference: <%= @booking.id %></p>
      </div>
      <div class="message-body">
        <h2>🚀 : <%= @booking.flight.rocket.name %></h2>
        <p>Model: <%= @booking.flight.rocket.model %></p>
        <h3>Pilot: <%= @booking.flight.rocket.user.name %></h3>

      </div>
      <div class="message-status">
        <div class="grid-btns">
          <h3 class="status"><%= @booking.status %></h3>
          <div class="btn-cancels">
            <%= link_to "Cancel", flight_booking_path(@booking.flight, @booking), method: :delete, class: 'btn btn-danger', data: {confirm: "are you sure you want to delete this booking?"} %>
          </div>
        </div>
      </div>
    </div>
  <% elsif current_user == @booking.user %>
    <div class="message-details">
      <div class="message-body">
        <h2>Departure date</h2>
        <h3><%= @booking.flight.date.strftime('%a %e %b %Y') %></h3>
      </div>
      <div class="message-body">
        <h2>🛫 <%= @booking.flight.departure %></h2>
        <h2>🛬 <%= @booking.flight.destination %></h2>
        <p>Booking reference: <%= @booking.id %></p>
      </div>
      <div class="message-status message-body">
        <h2 class="status">Status: <%= @booking.status %></h2>
      </div>
    </div>
    <div class="message">
      <div class="message-body">
        <h2>🚀 : <%= @booking.flight.rocket.name %></h2>
        <p>Model: <%= @booking.flight.rocket.model %></p>
        <%= cl_image_tag("#{@booking.flight.rocket.photo}",
              crop: :fill, class: "rocket-pic-booking") %>
        </div>
        <div class="message-body">
        <h3>Pilot: <%= @booking.flight.rocket.user.name %></h3>
      </div>

    </div>
  <% end %>


  <%= link_to "Check flight offer", flight_path(@booking.flight), class: 'btn btn-primary' %>

</div>
